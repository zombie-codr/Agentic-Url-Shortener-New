# Greenfield Scenario
Requirement: Build a URL shortener with create and redirect APIs.

Flow: Requirement analysis -> task plan -> architecture -> human approval -> implementation and test design -> synchronization -> validation -> security -> documentation -> release approval.

Primary evidence: dependency graph, parallel-ready nodes, two approval gates, audit events, generated engineering outputs.
